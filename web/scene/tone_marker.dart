part of tech3;

class marker
{
  int position_index; // the index of which freq bar the marker belongs to
  int height; // or freq
  int freq_box;
  int marker_id;
  bool hit = false;
  
  marker()
  {
    
  }
}